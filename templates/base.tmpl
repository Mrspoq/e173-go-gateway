{{define "base"}}
<!DOCTYPE html>
<html lang="en" class="h-full">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{.title}} - E173 Gateway</title>
    
    <!-- Tailwind CSS -->
    <link href="/static/bundle.css" rel="stylesheet">
    
    <!-- HTMX -->
    <script src="https://unpkg.com/htmx.org@1.9.10" integrity="sha384-D1Kt99CQMDuVetoL1lrYwg5t+9QdHe7NLX/SoJYkXDFfX37iInKRy5xLSi8nO7UC" crossorigin="anonymous"></script>
    
    <!-- _hyperscript for enhanced interactions -->
    <script src="https://unpkg.com/hyperscript.org@0.9.12"></script>
    
    <!-- Dark mode toggle script -->
    <script>
        // Dark mode toggle functionality
        if (localStorage.theme === 'dark' || (!('theme' in localStorage) && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
            document.documentElement.classList.add('dark')
        } else {
            document.documentElement.classList.remove('dark')
        }
        
        function toggleDarkMode() {
            if (document.documentElement.classList.contains('dark')) {
                document.documentElement.classList.remove('dark')
                localStorage.theme = 'light'
            } else {
                document.documentElement.classList.add('dark')
                localStorage.theme = 'dark'
            }
        }
    </script>
    
    <!-- Custom head content -->
    {{block "head" .}}{{end}}
</head>
<body class="h-full bg-gray-50 dark:bg-gray-900" hx-boost="true">
    <div class="min-h-full">
        <!-- Navigation -->
        {{template "nav" .}}
        
        <!-- Main content -->
        <main class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
            <div id="page" class="px-4 py-6 sm:px-0">
                {{block "content" .}}
                    {{template "dashboard_content" .}}
                    {{template "modems_content" .}}
                    {{template "sims_content" .}}
                    {{template "cdrs_content" .}}
                    {{template "blacklist_content" .}}
                {{end}}
            </div>
        </main>
    </div>
    
    <!-- Global notifications area -->
    <div id="notifications" class="fixed top-4 right-4 z-50"></div>
    
    <!-- Custom scripts -->
    {{block "scripts" .}}{{end}}
</body>
</html>
{{end}}
